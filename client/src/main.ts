// Phase 0 entry point — scaffold placeholder.
// Wired up properly in the "local play loop" step (parser -> model -> render -> input).

const status = document.getElementById("status");
if (status) {
  status.textContent = "Scaffold OK — game loop lands next.";
}
