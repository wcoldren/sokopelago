# Node / client build
node_modules/
client/dist/
client/.vite/
client/coverage/
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Local Claude Code session state
.claude/
sokopelago.apworld

# Local scratch / playtest notes (convention: scratch lives here, git-excluded)
notes/local/

# Offline corpus tooling: dev-only download caches (raw third-party sets, calibration data).
# Ingested .xsb + provenance ARE committed; these raw caches are not.
.ingest-cache/
data/calibration/

# Local build of the external SokoBoy solver (never vendored; see docs/EXTERNAL-SOLVER.md).
.sokoboy/
