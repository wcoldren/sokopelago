[mypy]
python_version = 3.11
strict = True
# AP core is not fully typed and isn't on the path outside the world job; don't let
# its untyped imports fail the run (matches AP CI's --follow-imports=silent).
follow_imports = silent
ignore_missing_imports = True
warn_unused_ignores = False
